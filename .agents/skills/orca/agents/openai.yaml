interface:
  display_name: "Orca"
  short_description: "Repo-specific patterns and workflows for orca"
  default_prompt: "Use the orca repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true